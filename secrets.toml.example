# Copy to .streamlit/secrets.toml locally, or paste into
# Streamlit Cloud -> app -> Settings -> Secrets. Never commit the real file.

# Screenshot reading (console.anthropic.com -> API keys)
ANTHROPIC_API_KEY = "sk-ant-..."
# CLAUDE_MODEL = "claude-sonnet-5"      # optional; claude-haiku-4-5-20251001 is cheaper

# Journal that survives Streamlit Cloud restarts (same private data repo as RSI Desk)
GITHUB_TOKEN = "github_pat_..."          # fine-grained, Contents: Read and write on DATA_REPO only
DATA_REPO = "buildbridgefl/rsi-desk-data"
# JOURNAL_PATH = "absorption/journal.csv"
